name: 'oee'
version: '1.0.0'
config-version: 2

profile: 'oee'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]
target-path: "target"
clean-targets: ["target", "dbt_packages"]

models:
  oee:
    staging:
      +materialized: view
      +schema: staging
    marts:
      +materialized: table
      +schema: marts
